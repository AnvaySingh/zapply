[project]
name = "apply-copilot"
version = "0.1.0"
description = "A personal, local, single-user job-application copilot — Employable's brain without its hands."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40",
    "openai>=1.40",
    "langfuse>=3.0,<4",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "typer>=0.12",
    "rich>=13.7",
    "httpx>=0.27",
    "feedparser>=6.0",
    "pyyaml>=6.0",
    "instructor>=1.5",
    "sentence-transformers>=3.0",
    "numpy>=1.26",
    "pypdf>=4.2",
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "python-multipart>=0.0.9",
]

[project.scripts]
apply-copilot = "apply_copilot.cli:app"

[dependency-groups]
dev = [
    "pytest>=8.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apply_copilot"]

[tool.pytest.ini_options]
testpaths = ["evals"]
